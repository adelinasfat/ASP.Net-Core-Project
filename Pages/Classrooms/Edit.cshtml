@page
@model Proiect.Pages.Classrooms.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Classroom</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Classroom.ID" />
            <div class="form-group">
                <label asp-for="Classroom.StudentID" class="control-label"></label>
                <select asp-for="Classroom.StudentID" class="form-control" asp-items="ViewBag.StudentID"></select>
                <span asp-validation-for="Classroom.StudentID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Classroom.ClassID" class="control-label"></label>
                <select asp-for="Classroom.ClassID" class="form-control" asp-items="ViewBag.ClassID"></select>
                <span asp-validation-for="Classroom.ClassID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
